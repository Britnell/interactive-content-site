---
import TeaserComp from "../react/teaser";

interface Props {
  blok: {
    component: string;
    [field: string]: any;
  };
}

const { blok } = Astro.props;
---

<TeaserComp blok={blok} client:visible />
